# homebrew-tap

Homebrew tap for [`gg`](https://github.com/gagarin-cloud/gg), the gagarin CLI.

```
brew install gagarin-cloud/tap/gg
```

Formulas in `Formula/` are generated and pushed by `gg`'s release pipeline
(GoReleaser) — don't hand-edit them.
